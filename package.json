{
  "name": "carshare-or-not",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node scripts/build-provider-data.js --check && node --test",
    "test": "node --test",
    "build:providers": "node scripts/build-provider-data.js",
    "build:og-image": "node scripts/build-og-images.js"
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "sharp": "^0.35.2"
  }
}
